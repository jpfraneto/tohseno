# TOHSENO 0.9.0 verification smoke intention

Create the smallest complete native iPhone application with no network access,
accounts, analytics, subscriptions, or external dependencies.

The initial screen must show the exact text `TOHSENO 0.9.0` and one button.
Tapping the button must change the displayed text to the exact text
`THE SHOT IS ALIVE`.

Include deterministic UI tests that launch the app, verify the initial text,
tap the button, and verify the changed text.
