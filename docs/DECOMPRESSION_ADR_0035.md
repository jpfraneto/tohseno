# Decompression — ADR 0035 / Tohseno 1.2

Jorge—

The shape that emerged is simpler than the machinery required to make it honest: a Shot enters once, may change forever, and a person can leave one non-transferable mark saying, “I encountered this exact software.” The governing decision is [`docs/adr/0035-claiming-software.md`](adr/0035-claiming-software.md). It adds no protocol encoding and does not touch the deployed generation-0.8 ABI.

The implementation is now on `main`. Commit `440fbc9c2fd64c437448b6f17c9acf9ac3b68419` contains the product; `f4eb7f245b4850eab77d99c3d0635c5bae9f8c7d` makes the Mac readiness test deterministic under CI scheduling; `f737386fab7f16582df3bf768c1c96b3a23816fe` installs the pinned Foundry toolchain in the final release gate. The target is **Tohseno 1.2.0 release candidate**, not stable. [CI run 33366216645](https://github.com/jpfraneto/tohseno/actions/runs/33366216645) is green across Rust, Swift/Xcode, Solidity, website, installers, release-integrity checks, and the isolated factory/Companion/network lifecycles.

What exists now: `TohsenoClaimsV1` is an immutable, separately deployable ERC-721/ERC-5192 receipt contract with one Claim per account per Shot, permanent non-transferability, finite/timed/open editions, current Registry-head binding, exact release/checkpoint/gesture commitments, ERC-1271 P-256 authorization, and relayer-paid gas. Companion draws and validates the privacy-minimized 64-point mark, waits for a canonical receipt, keeps the exact installation intention while the Mac sleeps, and later hands that release to the existing local build/install path. Registry now projects exactly one Ship followed by Updates, edition closure, private exact-Builder Follow, and relationship-backed private Updates—without follower counts, global Claim spam, wallet ceremony, or marketplace chrome.

Production was advanced only to the truthful dark boundary. Railway deployment `42ffc410-b11a-4d91-83a6-1a23144d90b0` serves the backward-compatible website from the implementation commit; its website tree is identical at the final source commit. [`tohseno.com/healthz`](https://tohseno.com/healthz) is healthy, the [Registry](https://tohseno.com/registry) is live, Registry writes remain unavailable, and both [Claims status](https://tohseno.com/api/registry/v1/claims/status) and a write probe fail closed with `503`. The unchanged Companion relay remains deployment `f16306bf-7894-4dfd-ba83-b388bad214cd`.

The active public witness was independently rechecked against Robinhood RPC: chain `4663`; Factory `0xb1bd208cd2af98e701f43d06aaa889d3a594df65`, runtime `0xb880403ff0da9a3f4c0c982cdea56fb198912c66bc436305e20861378f45c5f4`; Registry `0x3fe6508ba2660bc575080024f402c192a2e035a0`, runtime `0xfc80358a8f52ac8ae96691fecb611ade47447df79274e70defdd37989f3ca5e0`; activation digest `0x2b640260595def403343810d0dc4ee231e1faff427581be4f7b40cff4c189d28`.

The important decompression is what I did **not** turn into a claim. `TohsenoClaimsV1` has no production address, transaction, runtime hash, or signed activation yet. Therefore there is no production smoke Shot, claimant address, token ID, Claim number, Claim transaction, gesture commitment, claimed release, receipt URL, physical install, or proven later Update to report. The production-shaped offline-Mac, exact-release, receipt-substitution, private Follow, and Updates flows pass; physical evidence does not exist. No 1.2 DMG was built, notarized, published, or advertised, and the prior installer state was not relabeled.

The remaining boundary is deliberately human: bring the governed deployment signer into an owner-attended clean checkout, deploy the exact constructor-bound Claims contract once, obtain the existing 2-of-3 offline activation signatures, pin and redeploy every verifier, then use two real Companion identities plus a Mac and iPhones to prove Claim → offline preparation → recipient-signed install → later Update. Only after that should the constrained relayer and a signed/notarized 1.2 artifact become public truth.

The room is built. The lights are off until the people and evidence arrive.
