# Frozen product history

This directory preserves superseded product instructions for historical
reference. They do not define the current CLI or Studio behavior.

Canonical protocol rules remain in `protocol/`; accepted architecture decisions
remain in `docs/adr/`. Some root-level historical files are retained at their
original paths because frozen release manifests and verification scripts name
those exact paths.
