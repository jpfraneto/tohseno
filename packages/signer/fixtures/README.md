# Signer fixtures

The tracked envelope is regenerated from explicitly public fixture input with
the current deterministic test signer. It is a real Ed25519 verification
vector, but no private key or production key-derivation mechanism is persisted
in this repository.
